{
  "name": "living-memory-lab-profile",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Config-driven generator for Zhiyuan's bilingual GitHub profile.",
  "scripts": {
    "generate": "node scripts/generate-profile.mjs",
    "check": "node scripts/generate-profile.mjs --check",
    "test": "npm run check"
  },
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "yaml": "^2.8.1"
  }
}
